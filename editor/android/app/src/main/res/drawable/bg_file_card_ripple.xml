<?xml version="1.0" encoding="utf-8"?>
<!-- Ripple clipped to the card's rounded corners, so the press feedback
     doesn't spill past the card edges like the default rectangular ripple. -->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorAccentSecondary">
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent"/>
            <corners android:radius="16dp"/>
        </shape>
    </item>
</ripple>
